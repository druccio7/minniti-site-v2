import React from "react";

export default class PreContainer extends React.Component{
    render(){
        console.log(this.props);
        return(
            <div id="preContainer">preContainer</div>
        );
    }
}